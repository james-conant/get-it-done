import React, { useState } from "react";
import TodoForm from "./components/TodoForm";

const App = (props) => {
  return (
    <div className="app">
      <TodoForm />
    </div>
  );
};

export default App;
